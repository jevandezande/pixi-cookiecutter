[global]

disable = [
    "MD013",  # line length
    "MD033",  # no inline HTML
]

exclude = [
    ".git",
    ".pixi",
    "node_modules",
    "vendor",
    "dist",
    "build",
    "**/data/AGENTS_README.md",
]

respect-gitignore = true

[per-file-ignores]
".claude/**/*.md" = ["MD041"]  # skill files start with instructions, not headings
